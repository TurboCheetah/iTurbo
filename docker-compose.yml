version: '3.3'
services:
    iturbo:
        build: .
        container_name: iTurbo
        restart: always
    iturbo-db:
        image: "postgres"
        environment: 
          - POSTGRES_USER=turbo
          - POSTGRES_PASSWORD=turbodb
          - POSTGRES_DB=miyako
        volumes:
          - database-data:/home/turbo/iTurbo/db

volumes:
  database-data: